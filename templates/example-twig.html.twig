{% extends 'base.html.twig' %}

{% block body %}
    <div class="content">
        <div class="left column">
            <p>Avant :</p>
            <pre>{{ text }}</pre>
        </div>
        <div class="right column">
            <p>Après :</p>
            <pre>{{ text|typography }}</pre>
        </div>
    </div>
    
    <div class="content">
        <div class="left column">
            {{ text|raw }}
        </div>
        <div class="right column">
            {{ text|raw|typography }}
        </div>
    </div>
{% endblock %}
